# ChitraFrame Growth Agent — Bundle

This folder is a **drop-in scaffold** for the `Kbs-sol/PhotoFramePFS` repo. Copy the contents into the repo root, preserving paths:

```
docs/agent-bundle/.github/  →  PhotoFramePFS/.github/
docs/agent-bundle/.agent/   →  PhotoFramePFS/.agent/
docs/agent-bundle/docs/     →  PhotoFramePFS/docs/
```

Then follow `docs/AGENT_SETUP.md` for the 45-minute one-time setup.

**Do not run these workflows against this Lovable project — they target `PhotoFramePFS`.**
